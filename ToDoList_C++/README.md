# To Do List
  Learned file manipulation
# How to use
  1. Clone the repository in your machine
  2. open the terminal and type `g++ todo.cpp -o out` then `./out`
  3. A list will appear in your terminal with option to add/remove a task, Check/Uncheck a task
